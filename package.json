{
  "scripts": {
    "start": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "better-auth": "^1.6.5",
    "zustand": "^5.0.12"
  }
}
